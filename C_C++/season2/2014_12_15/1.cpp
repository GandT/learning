#include <stdio.h>#include <iostream>using namespace std;//グローバル変数int X , Y , E , x , y ;bool D[100][100];		//xとyの隣接リストint main(){	//上の頂点数、下の頂点数、棒の数を受け取る	cin >> X >> Y >> E;		//入力を受け取り隣接リストを完成させる	for(int i = 0;i < E;++i){		//x側の頂点とy側の頂点を受け取る		cin >> x >> y;				//上(x)→下(y)の向きに隣接リストを設定 		D[x][y] = true;	}		/*****マッチング計算*****/	//現在のペアを初期化(-1を代入)	fill(PY,PY+Y,1);		//マッチ数カウント変数	int match = 0;		//X側から順番に試す	for(int x = 0;x < X;++x){//		//初期化//		fill(V,V+X,false);//		//		if(match(x))++count;	}}