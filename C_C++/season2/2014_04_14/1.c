main(){	int N,S = 0;				//与えられた変数	int maxS,minS,Ss = 0;		//Sの最大値・最小値・合計値	int i;		while(1){				scanf("%d",&N);		if(N == 0)break;				//全員の合計を算出する		for(i = 0;i < N;++i){			scanf("%d",&S);						if(i == 0)maxS = minS = S;						Ss += S;			if(maxS < S)maxS = S;			if(minS > S)minS = S;						//printf("Ss:%d/n",Ss);		}				//合計から最大と最小をのぞく		Ss -= maxS;		Ss -= minS;				//平均点を出力する		printf("%d\n",Ss / (N - 2));				//変数の初期化		Ss = 0;	}}