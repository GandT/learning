#include <stdio.h>#include <iostream>#include <algorithm>using namespace std;int dataset;		//データセット数int hous;			//家の数int elec;			//発電機の数int house[100000];	//家の座標int inter[ 99999];	//家と家との間隔int sumel;			//電線の合計の長さint main(void){	//データセット数を受け取る	scanf("%d",&dataset);		/*****データセット数の回数分ループ*****/	for(int mainloop = 0 ; mainloop < dataset ; ++mainloop )	{		//家の数を受け取る		cin >> hous;				//発電機の数を受け取る		cin >> elec;				//家の座標を受け取る		for(int i = 0;i < hous;++i)cin >> house[i];				//家と家との間隔を計算		for(int i = 0;i < hous - 1;++i)inter[i] = house[i + 1] - house[i];				//家と家の間隔を昇順にソート		sort(inter,inter + (hous - 1));				//電線の合計の長さを算出し出力		if(hous <= elec)printf("0\n");		else {			for(int i = 0;i < hous - elec;++i)sumel += inter[i];			printf("%d\n",sumel);		}				//変数の初期化		sumel = 0;	}}